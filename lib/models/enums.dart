enum PointType {
  breakfast,
  lunch,
  dinner,
  snack,
  activity,
  food
}

enum Gender {
  male,
  female,
}

enum Movement {
  no,
  some,
  much,
  daily,
}

enum Goal {
  hold,
  reduce,
}

enum TextFieldType {
  integer,
  decimal,
  text,
}
